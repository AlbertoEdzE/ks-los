{
  "annotations": {
    "list": []
  },
  "panels": [
    {
      "type": "graph",
      "title": "Risk Inference Total",
      "targets": [
        {
          "expr": "risk_inference_total",
          "legendFormat": "inferences"
        }
      ]
    },
    {
      "type": "graph",
      "title": "API Requests by Endpoint (rate)",
      "targets": [
        {
          "expr": "sum by (endpoint) (rate(requests_total[5m]))",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "type": "graph",
      "title": "API Errors by Endpoint (rate)",
      "targets": [
        {
          "expr": "sum by (endpoint) (rate(request_errors_total[5m]))",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Inference Latency (seconds)",
      "targets": [
        {
          "expr": "rate(inference_latency_seconds_sum[5m]) / rate(inference_latency_seconds_count[5m])",
          "legendFormat": "latency_avg"
        }
      ]
    },
    {
      "type": "graph",
      "title": "API Latency by Endpoint (seconds)",
      "targets": [
        {
          "expr": "sum by (endpoint) (rate(request_latency_seconds_sum[5m])) / sum by (endpoint) (rate(request_latency_seconds_count[5m]))",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Training Runs Total",
      "targets": [
        {
          "expr": "training_runs_total",
          "legendFormat": "training_runs"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Drift Runs Total",
      "targets": [
        {
          "expr": "drift_runs_total",
          "legendFormat": "drift_runs"
        }
      ]
    }
  ],
  "schemaVersion": 38,
  "title": "KS LOS Basic",
  "version": 1
}
